module_Business = true;

// Server-side fuel station business config (defaults; can be overridden by mission config vars)
if (isNil "dtk_fuelProfitRate") then { dtk_fuelProfitRate = 0.10; };
if (isNil "dtk_fuelStationBuyPrice") then { dtk_fuelStationBuyPrice = 250000; };
if (isNil "dtk_fuelStockMax") then { dtk_fuelStockMax = 400; };
if (isNil "dtk_fuelBarrelLiters") then { dtk_fuelBarrelLiters = 100; };

business_fuelStations = [];

